% AUTO-GENERATED by scripts/build_audit_supplement_tables_v2.py.
% Do not edit by hand; rebuild from the frozen evidence artifacts.
\begingroup
\fontsize{7.0}{8.0}\selectfont
\setlength{\tabcolsep}{1.5pt}
\renewcommand{\arraystretch}{1.12}
\begin{longtable}{@{}p{.035\textwidth}p{.310\textwidth}p{.310\textwidth}p{.290\textwidth}@{}}
\caption{Normative evidence-to-wording truth table. A plugin row is conditional on a registered checker; absent such a checker, the validator fails closed.}\label{tab:supp-truth-table}\\
\toprule
ID & Evidence condition & Expected tuple & Required action \\
\midrule
\endfirsthead
\multicolumn{4}{l}{\itshape Table \thetable\ continued}\\
\toprule
ID & Evidence condition & Expected tuple & Required action \\
\midrule
\endhead
\midrule \multicolumn{4}{r}{\itshape Continued on next page}\\
\endfoot
\bottomrule
\endlastfoot
\texttt{T01} & Window claim; window accountant; exact adjacency, sampler, schedule, clipping/noising, and runtime match & K=1; \texttt{DIRECT}; \texttt{ALLOWED}; \texttt{EVIDENCE\_\allowbreak VALIDATED} & State window-level DP and the accountant adjacency. \\
\texttt{T02} & Same as T01 but runtime binding is missing & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Bind a matched runtime trace before emitting any copy-ready statement about the concrete run. \\
\texttt{T03} & Window claim with sampler/accountant mismatch & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & No positive DP sentence. Name the sampler mismatch. \\
\texttt{T04} & Window claim with non-finite/negative epsilon or delta outside (0,1) & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Reject the report before unit reasoning. \\
\texttt{T05} & Event claim; observed event multiplicity one; no transformation-stability proof & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Observed non-overlap is not sufficient. \\
\texttt{T06} & Event claim; validated K=1; raw and accountant adjacency metrics match & K=1; \texttt{DIRECT}; \texttt{ALLOWED}; \texttt{EVIDENCE\_\allowbreak VALIDATED or conditional} & Reuse the accountant number and state the stability condition. \\
\texttt{T07} & Replace-one raw event changes one generated record; accountant uses add/remove adjacency & K=2; \texttt{CONVERT}; \texttt{ALLOWED if nonvacuous}; \texttt{EVIDENCE\_\allowbreak VALIDATED or conditional} & Apply the built-in K=2 group conversion; do not reuse the same number. \\
\texttt{T08} & Event claim; validated K>1; built-in conversion has delta\_K<1 & K=K; \texttt{CONVERT}; \texttt{ALLOWED}; \texttt{EVIDENCE\_\allowbreak VALIDATED or conditional} & Print recomputed epsilon\_K, delta\_K, and the metric used for K. \\
\texttt{T09} & Event claim; validated K>1; built-in conversion has delta\_K>=1 & K=K; \texttt{CONVERT}; \texttt{BLOCKED\_\allowbreak VACUOUS}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Show the diagnostic conversion but emit no copy-ready DP claim. \\
\texttt{T10} & Owner claim; native owner sampler, one owner vector before clipping/noising, owner accountant, matched owner adjacency & K=1; \texttt{DIRECT}; \texttt{ALLOWED}; \texttt{EVIDENCE\_\allowbreak VALIDATED or conditional} & State owner-level DP and every native-owner premise. \\
\texttt{T11} & Owner claim; window accountant; owner removal changes one generated record and no other pipeline state & K=1; \texttt{DIRECT}; \texttt{ALLOWED}; \texttt{EVIDENCE\_\allowbreak VALIDATED or conditional} & Reuse only under the validated add/remove owner-to-window metric. \\
\texttt{T12} & Owner claim; window accountant; validated owner distance K>1; nonvacuous group conversion & K=K; \texttt{GROUP}; \texttt{ALLOWED}; \texttt{EVIDENCE\_\allowbreak VALIDATED or conditional} & Print recomputed group parameters and the contribution bound. \\
\texttt{T13} & Owner claim; group conversion is vacuous & K=K; \texttt{GROUP}; \texttt{BLOCKED\_\allowbreak VACUOUS}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Recommend native owner accounting, a public contribution bound, or narrower wording. \\
\texttt{T14} & Multi-owner record with complete conservative attribution and a registered relational stability checker & K=plugin; \texttt{According to checker}; \texttt{According to checker}; \texttt{According to evidence} & Charge every attributed owner; the built-in owner-partition checker rejects attribution arity above one. \\
\texttt{T15} & Multi-owner or ambiguous record encoded as one arbitrary owner or pseudo-owner & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Require complete attribution or a registered relational analysis. \\
\texttt{T16} & Private global normalization or private adaptive selection is unaccounted & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Use public/local preprocessing, separately DP preprocessing, or a validated global stability bound. \\
\texttt{T17} & Separately DP preprocessing with a registered release/composition graph & K=plugin; \texttt{According to registered graph}; \texttt{According to registered graph}; \texttt{EVIDENCE\_\allowbreak VALIDATED} & Use only the registered composition or post-processing rule. \\
\texttt{T18} & Separately DP preprocessing is named but its release/composition graph is absent & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{EXTERNAL\_\allowbreak UNVERIFIED} & Do not guess composition. \\
\texttt{T19} & Fixed mapping produced by a public deterministic policy and bound to the run & K=K; \texttt{According to K}; \texttt{According to conversion}; \texttt{According to runtime} & Normal fixed-mapping path. \\
\texttt{T20} & Mapping is merely the observed result of a private adaptive policy & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & An observed branch cannot certify worst-case stability. \\
\texttt{T21} & union mode has an executable complete branch-union construction & K=K; \texttt{According to K}; \texttt{According to conversion}; \texttt{EVIDENCE\_\allowbreak VALIDATED or conditional} & Record the union-construction id and digest. \\
\texttt{T22} & union is only a user-selected label over an observed mapping & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Reject the claimed union bound. \\
\texttt{T23} & stochastic\_bound has only user integers or Monte Carlo evidence & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Retain diagnostics only. \\
\texttt{T24} & stochastic\_bound has a registered uniform theorem, verified beta, and delta rule & K=plugin; \texttt{According to plugin}; \texttt{According to plugin}; \texttt{EVIDENCE\_\allowbreak VALIDATED} & Print the bound, beta, and exact unconditional/conditional convention. \\
\texttt{T25} & Arbitrary external-conversion JSON has complete-looking fields but no registered checker & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{EXTERNAL\_\allowbreak UNVERIFIED} & Record it as an attachment but emit no positive wording. \\
\texttt{T26} & Registered same-mechanism conversion matches every bound field and recomputes successfully & K=plugin; \texttt{CONVERT or GROUP}; \texttt{According to plugin}; \texttt{EVIDENCE\_\allowbreak VALIDATED} & Print the registered method and bound pipeline digest. \\
\texttt{T27} & Registered native raw-unit mechanism matches sampling, clipping, noising, adjacency, and accounting units & K=1; \texttt{DIRECT}; \texttt{ALLOWED}; \texttt{EVIDENCE\_\allowbreak VALIDATED or conditional} & State the native raw-unit mechanism, not the old window mechanism. \\
\texttt{T28} & Mapping digest is missing or differs from the privacy report & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Regenerate the mapping/report pair. \\
\texttt{T29} & CSV hash matches but transform, split, code, or support parameters are not bound & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Require a pipeline digest. \\
\texttt{T30} & Sample rate or step count changes under raw adjacency and no accountant covers the change & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Fix a public schedule/population or attach a matching analysis. \\
\texttt{T31} & Model selection uses private data but is not public, composed, or separately accounted & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Narrow the claim to the covered run or account for selection. \\
\texttt{T32} & Accountant contract passes but DP noise uses a research PRNG & K=K; \texttt{According to K}; \texttt{RESEARCH\_\allowbreak ONLY}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Report accountant diagnostics; do not emit evidence-validated wording for the concrete run. \\
\texttt{T33} & Public deterministic DP-noise seed, or missing/unknown randomness evidence & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID for public deterministic; otherwise BLOCKED\_\allowbreak UNVERIFIED}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Rerun with a registered private CSPRNG mechanism whose exact implementation and runtime evidence are bound. \\
\texttt{T34} & Provenance metadata is classified private\_internal & K=K; \texttt{Underlying path retained}; \texttt{According to underlying validation}; \texttt{According to underlying validation} & Emit only the redacted public certificate; omit scenario, digests, counts, and multiplicities. \\
\texttt{T35} & Base accountant identifier has no registered recomputation checker & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak UNVERIFIED}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Retain the accountant output as an attachment but authorize no privacy sentence. \\
\texttt{T36} & Registered accountant version or recomputed epsilon differs from the report & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Regenerate with the registered version and exact accountant inputs. \\
\texttt{T37} & Mapping generated\_unit, manifest generated-record unit, and accounting unit differ & K=---; \texttt{UNDERSPECIFIED}; \texttt{BLOCKED\_\allowbreak INVALID}; \texttt{DIAGNOSTIC\_\allowbreak ONLY} & Export the actual generated records; do not relabel a window mapping as a native raw-unit mechanism. \\
\end{longtable}
\endgroup
