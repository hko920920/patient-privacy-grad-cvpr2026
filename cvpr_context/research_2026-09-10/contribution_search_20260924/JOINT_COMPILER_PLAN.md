# One joint post-release compiler candidate

Exploratory, selected from the mechanism investigation, not a preregistered paper result.

Final bounded operation: combine the DINO source functional target and the ResNet18 publicly transported functional target in one soft-label solve on each of the two existing feature-DP PNG banks. DenseNet is excluded from this solve. It remains a previously used development receiver, not a final unseen architecture.

No new Q, DP release, pixel training, raw V images or Reserved. Equal 1/2 weights after normalizing each public-P score residual by its target RMS. Same trace-scaled 0.001 label prior and bounds [-1,1]. No coefficient or receiver sweep. Contrast against original hard labels, ordinary source relabeling, source-only label solve, and ResNet18-only transported label solve; all two releases retained.

Question: does adding heterogeneous PUBLIC information after an unchanged source-DP release improve an image/label packet beyond source-only functional label distillation? A positive result is a candidate method signal, not final novelty or generalization proof.

