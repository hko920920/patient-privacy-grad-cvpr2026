# Fixed-image compiler controls and inference

Written after the four initial transferred-label results; explicitly exploratory, no retrospective preregistration claim.

Need distinguish transported protected signal from generic label correction. On both fixed DP feature PNG banks and both calibration receivers, use the SAME bounded-label least-squares solver and SAME fixed regularizer to compile:
1. transported corresponding DP target (already completed);
2. transported PUBLIC target (same maps, no private target in labels; images remain DP-derived);
3. direct REAL-P classifier target (no protected target in labels; images remain DP-derived).

No pixel optimization, new DP release, new encoder, new raw Q, Expert/Reserved, or V fitting. Retain every direction and comparison. Compute fixed-bank paired bootstrap with the existing 2,000 patient draws for new DP labels versus original labels and versus each public label-control, emphasizing the other receiver (not used for label fitting). These are conditional, exploratory intervals.

